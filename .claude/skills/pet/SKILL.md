---
description: Toggle the terminal pet (status line blob) on or off. Use when the user wants to turn their pet on, off, or check its state.
argument-hint: [on|off|toggle|status]
disable-model-invocation: true
---

Run `/Users/yili/Documents/yoyo/claudePet/pet.sh $ARGUMENTS` with the Bash tool. If the user gave no arguments, run it with no arguments (it toggles). Then tell the user whether the pet is now on or off.
